<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta property="og:title" content="{{ title or meta.title }}" />
    <meta property="og:description" content="{{ description or excerpt or meta.description }}" />
    <meta property="og:url" content="{{ site.url }}{{ page.url }}" />
    <meta property="og:type" content="website" />
    <title>{{ title or metadata.title }}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="{{ '/css/main.css' | url }}">
    <link rel="stylesheet" href="https://unpkg.com/lucide-static@latest/font/lucide.css"/>
    <script src="https://unpkg.com/lucide@latest"></script>
    <style>
        :root {
            --font-heading: 'Space Grotesk', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            --font-body: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        }
    </style>
</head>
<body>
    <div class="container">
        <nav class="navbar">
            <a href="/" class="navbar-brand">
                <i data-lucide="scales"></i>
                Mahakama API
            </a>
            <div class="nav-links" style="display: flex; gap: 0.75rem; flex-wrap: wrap;">
                <a href="/api-reference" class="btn btn-indigo">
                    <i data-lucide="code"></i>
                    API Reference
                </a>
                <a href="/architecture" class="btn btn-purple">
                    <i data-lucide="layout-dashboard"></i>
                    Architecture
                </a>
                <a href="/deployment" class="btn btn-cyan">
                    <i data-lucide="rocket"></i>
                    Deployment
                </a>
                <a href="https://github.com/Emmanuel-Melon/mahakama-api" class="btn btn-emerald" target="_blank" rel="noopener">
                    <i data-lucide="github"></i>
                    GitHub
                    <i data-lucide="external-link" width="14" height="14"></i>
                </a>
            </div>
        </nav>

        <main class="content">
            {{ content | safe }}
        </main>

        <footer style="margin-top: 6rem; padding: 2rem 0; color: var(--gray-500); font-size: 0.9rem;">
            <div style="display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 1rem; max-width: var(--max-width); margin: 0 auto; padding: 0 1rem;">
                <p style="margin: 0;">© 2025 Mahakama Legal Assistant</p>
                <p style="margin: 0; display: flex; align-items: center; gap: 0.5rem;">
                    <span>Created with</span>
                    <span style="color: #e53e3e;">❤️</span>
                    <span>by</span>
                    <a href="https://github.com/Emmanuel-Melon" 
                       target="_blank" 
                       rel="noopener" 
                       style="display: inline-flex; align-items: center; gap: 0.25rem; color: var(--gray-700); text-decoration: none; transition: color 0.2s ease;">
                        <i data-lucide="github" width="14" height="14"></i>
                        <span>Emmanuel Gatwech</span>
                    </a>
                </p>
            </div>
        </footer>
    </div>

    <script>
        // Initialize Lucide icons after the DOM is fully loaded
        document.addEventListener('DOMContentLoaded', function () {
            lucide.createIcons();
        });
    </script>
</body>
</html>

